from setuptools import setup

setup(name='nch_distributions',
      version='0.4',
      description='Gaussian distributions',
      packages=['nch_distributions'],
      author='NCH',
      zip_safe=False)
