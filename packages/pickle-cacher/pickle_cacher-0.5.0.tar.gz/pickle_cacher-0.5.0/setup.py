#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['pickle_cacher']

package_data = \
{'': ['*']}

install_requires = \
['atomic-write-path >= 1.0', 'wrapt >= 1.12']

setup(name='pickle_cacher',
      version='0.5.0',
      description='Pickle-based caching.',
      author='Bao Wei',
      author_email='baowei.ur521@gmail.com',
      url=None,
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
     )
