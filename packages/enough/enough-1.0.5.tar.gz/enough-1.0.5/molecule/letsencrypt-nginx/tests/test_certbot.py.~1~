testinfra_hosts = ['bind-host']

def test_certs(host):
    with host.sudo():
        host.run("apt-get install -y curl")
    cmd = host.run("curl -m 5 -I https://certbot-host.$(hostname -d)")
    assert cmd.rc == 0
