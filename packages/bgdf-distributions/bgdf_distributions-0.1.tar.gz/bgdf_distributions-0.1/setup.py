from setuptools import setup

setup(name='bgdf_distributions',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['bgdf_distributions'],
      zip_safe=False)
