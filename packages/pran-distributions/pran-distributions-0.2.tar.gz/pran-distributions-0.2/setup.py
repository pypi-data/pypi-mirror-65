from setuptools import setup

setup(name='pran-distributions',
      version='0.2',
      description='Gaussian distributions',
      packages=['pran-distributions'],
      authors = ['Pranav Aggarwal'],
      zip_safe = False)
