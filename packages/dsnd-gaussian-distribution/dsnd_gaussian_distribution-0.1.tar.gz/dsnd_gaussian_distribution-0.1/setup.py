from setuptools import setup

setup(name='dsnd_gaussian_distribution',
      version='0.1',
      description='Gaussian distributions',
      packages=['dsnd_gaussian_distribution'],
      zip_safe=False)
