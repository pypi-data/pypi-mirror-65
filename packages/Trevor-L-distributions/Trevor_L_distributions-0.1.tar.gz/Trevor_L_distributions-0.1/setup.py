from setuptools import setup

setup(name='Trevor_L_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['Trevor_L_distributions'],
      author = 'Trevor LaViale',
      zip_safe=False)

