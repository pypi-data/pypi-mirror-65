from setuptools import setup

setup(name='nch-distributions-1',
      version='0.1',
      description='Gaussian distributions',
      packages=['nch-distributions-1'],
      author='NCH',
      zip_safe=False)
