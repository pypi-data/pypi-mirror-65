from setuptools import setup

setup(name='distributions-of-probability',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions-of-probability'],
      zip_safe=False)
