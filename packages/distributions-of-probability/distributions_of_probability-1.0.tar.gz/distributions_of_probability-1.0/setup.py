from setuptools import setup

setup(name='distributions_of_probability',
      version='1.0',
      description='Gaussian distributions',
      packages=['distributions_of_probability'],
      zip_safe=False)
